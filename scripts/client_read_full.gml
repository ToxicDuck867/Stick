_otherexists=false;otherid=readushort();with (Dummy){  if (gameid==other.otherid)  {    x=readudecint();    y=readdecshort();    right=readbyte();    dir=readdecshort();    vspd=readdecshort();    spriteSet=readbyte();    _spr=readushort();    if (sprite_exists(_spr))      sprite_index=_spr;    image_index=readtwodecshort();    sprite_speed=readtwodecshort();    hat=readushort();    class=readbyte();    pvp=readbyte();    name=readstring();    clan=readstring();    if (bytesleft())    {      knockDir=readdecshort();    }    else    {      knockDir=0;    }    init_sprite(spriteSet);    alarm[2]=RANGE_TIME;    other._otherexists=true;  }}if ((_otherexists==false) && (otherid!=global.myid)){  with (instance_create(readudecint(),readdecshort(),Dummy))  {    gameid=other.otherid;    right=readbyte();    dir=readdecshort();    vspd=readdecshort();    spriteSet=readbyte();    _spr=readushort();    if (sprite_exists(_spr))      sprite_index=_spr;    image_index=readtwodecshort();    sprite_speed=readtwodecshort();    hat=readushort();    class=readbyte();    pvp=readbyte();    name=readstring();    clan=readstring();    if (bytesleft())    {      knockDir=readdecshort();    }    else    {      knockDir=0;    }    init_sprite(spriteSet);        if (name=="")    {      client_send_request(other.otherid);    }  }}