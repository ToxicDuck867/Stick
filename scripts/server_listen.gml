if ((CAN_HOST=true) && (file_exists("logs\banlist.txt"))){    var _file;  global.banList=ds_list_create();  _file=file_text_open_read("logs\banlist.txt");  while (!file_text_eof(_file))  {    ds_list_add(global.banList,file_text_read_string(_file));    file_text_readln(_file);  }    file_text_close(_file);    clan_load();    global.accountlisten=tcplisten(ACCOUNT_TCP,100,true);  if (global.accountlisten<=0)  {    show_message("Failed to create a listening socket (ACCOUNT_TCP)");    return (false);  }  /*global.chatlisten=tcplisten(CHAT_TCP,20,true);  if (global.chatlisten<=0)  {    show_message("Failed to create a listening socket (CHAT_TCP)");    game_end();    exit;  }*/  global.gamelisten=tcplisten(TCP1,100,true);  if (global.gamelisten<=0)  {    show_message("Failed to create a listening socket (TCP1)");    return (false);  }  global.udp=udpconnect(SERVER_UDP1,true);  if (global.udp<=0)  {    show_message("Failed to access the requested UDP port (SERVER_UDP1).");    return (false);  }  global.server=true;  global.npc_index=0;  return (true);}return (false);