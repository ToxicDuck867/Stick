if ((readbyte()==250) && (class==250)){  _succeed=false;  otherid=readstring();  if (name!=otherid)   {    with (Dummy)    {      if (string_lower(name)==string_lower(other.otherid))      {        other._succeed=true;        instance_destroy();      }    }  }  if (_succeed)  {    server_send_message(otherid+" has been kicked");  }}else{  BreakEngine.secure=false;  BreakEngine.error=113.;  exit;}