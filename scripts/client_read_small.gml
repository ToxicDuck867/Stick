_otherexists=false;otherid=readushort();with (Dummy){  if (gameid==other.otherid)  {    x=readudecint();    y=readdecshort();    dir=readdecshort();    vspd=readdecshort();    if (bytesleft())    {      knockDir=readdecshort();    }    else    {      knockDir=0;    }    alarm[1]=DISC_TIME+200;    alarm[2]=RANGE_TIME;    other._otherexists=true;  }}if ((_otherexists==false) && (otherid!=global.myid)){  client_send_request(otherid);}