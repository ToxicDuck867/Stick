otherid=readushort();_damage=readushort();_daze=readbyte();_sound=readushort();_horizKnock=readdecshort();_vertKnock=readdecshort();if (global.myid==otherid){  if (!Player.invulnerable)  {    set_hp(global.hp-damage_calculate_receive(_damage));    if (Player.alarm[6]>0)    {      Player.alarm[6]=global.hpRegenTime*3;    }    Player.dazed=true;    Player.image_alpha=.5;    Player.alarm[3]=_daze;    if (Player.pvp)    {      pvpToggle=false;      Player.alarm[8]=Player.pvpDelay;    }    if (_sound!=false)    {      if ((global.sound) && (sound_exists(_sound)))      {        sound_play(_sound);      }    }    if (_horizKnock!=0)      Player.knockDir=knock_calculate(_horizKnock);    if (_vertKnock!=0)      Player.vspd+=knock_calculate(_vertKnock);    if ((_horizKnock!=0) || (_vertKnock!=0))    {      with (Player)      {        client_send_small(false);        set_resynch();      }      with (attack)      {        instance_destroy();      }    }  }}else{  with (Dummy)  {    if (gameid==other.otherid)    {      dazed=true;      image_alpha=.5;      alarm[3]=other._daze;      if (other._sound!=false)      {        if ((global.sound) && (sound_exists(other._sound)))        {          sound_play(other._sound);        }      }    }  }}