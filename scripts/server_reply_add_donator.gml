if ((readbyte()==250) && (class==250)){  if (SITE_CONNECT)  {    otherid=readstring();    if ((file_exists("accounts\"+otherid+".so")) &&      (ds_list_find_index(AccountServer.donatorList,string_lower(otherid))==-1))    {      ds_list_add(AccountServer.donatorList,string_lower(otherid));      ds_list_add(AccountServer.donatorStats,"("+string(ds_list_size(AccountServer.donatorList))+", '"+otherid+"', 0, 0, 0, 0, 0, 0, 0, 0)");      donators_save();            with (Dummy)      {        if (class==250)        {          server_send_notice(other.otherid+" has been added to the donator list");        }      }    }  }  else  {    server_send_notice("Site Connections are currently disabled");  }}else{  BreakEngine.secure=false;  BreakEngine.error=116.;  exit;}