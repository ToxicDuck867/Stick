otherid=readushort();_damage=readushort();_daze=readbyte();_sound=readushort();_horizKnock=readdecshort();_vertKnock=readdecshort();clearbuffer();writebyte(5);writeushort(otherid);writeushort(_damage);writebyte(_daze);writeushort(_sound);writedecshort(_horizKnock);writedecshort(_vertKnock);with (Dummy){  if (gameid==other.otherid)  {    server_send_local(id,false);    dazed=true;    image_alpha=.5;    alarm[3]=other._daze;    /*if (other._horizKnock!=0)      knockDir=other._horizKnock;    if (other._vertKnock!=0)      vspd+=other._vertKnock;*/  }}